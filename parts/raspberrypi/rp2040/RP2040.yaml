# Dual Cortex-M0+ microcontroller. The datasheet uses "RP2040" as the
# chip's only name with no separate ordering-suffix scheme (unlike e.g.
# ST's STM32xxx naming); no distinct MPN was found beyond the chip name
# itself in the sections checked.
#
# lifecycle.status: active is not an explicit datasheet statement (no
# EOL/NRND notice was found, but that section wasn't exhaustively
# checked across all 642 pages) -- treated as a reasonable inference
# given RP2040 remains the current, widely-sold Raspberry Pi Pico MCU as
# of this datasheet's 2025-02-20 build date.
schema_version: "0.1"
kind: part
id: raspberrypi/RP2040

manufacturer: raspberrypi
mpn: RP2040

device: raspberrypi/RP2040
package: standards/QFN56-RP2040

existence:
  status: verified

lifecycle:
  status: active

sources:
  - raspberrypi/RP-008371-DS@rev1
